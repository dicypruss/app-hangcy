# Hangcy

Android app that rejects incoming calls per inserted SIM. If the system does not report which SIM received the call, a single toggle rejects all incoming calls. Lines with the toggle off are left unchanged.
